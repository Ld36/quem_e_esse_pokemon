import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'quem_e_esse_pokemon_novo',
  webDir: 'www'
};

export default config;
